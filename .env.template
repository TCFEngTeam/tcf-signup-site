# ── Secrets (never commit real values) ──
HUBSPOT_API_KEY=
UNREGISTER_TOKEN_SECRET=
RESEND_API_KEY=
RESEND_FROM_EMAIL=TCF Trainings <trainings@yourdomain.com>
STAFF_NOTIFY_EMAIL=staff@yourdomain.com

# ── Deployment ──
NEXT_PUBLIC_APP_URL=http://localhost:3000

# HubSpot portal wiring (pipelines, property names, association labels) lives in
# config/hubspot.json — edit that file when HubSpot settings change.
